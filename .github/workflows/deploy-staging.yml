name: CI/CD Pipeline

on:
  push:
    branches:
      - staging

jobs:
  print-secret:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Print secret
        run: echo "Test Secret is ${{ secrets.SERVER }}"
